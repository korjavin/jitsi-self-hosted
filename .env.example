# Security
#
# Set these to strong passwords to avoid intruders from impersonating a service account
# The service(s) won't start unless these are specified
# Running ./gen-passwords.sh will update .env with strong passwords
JICOFO_COMPONENT_SECRET=
JICOFO_AUTH_PASSWORD=
JVB_AUTH_PASSWORD=
JIGASI_XMPP_PASSWORD=
JIBRI_RECORDER_PASSWORD=
JIBRI_XMPP_PASSWORD=

# System time zone
TZ=UTC

# Public URL for the web service (required)
# IMPORTANT: Change this to your actual domain
PUBLIC_URL=https://jitsi.${JITSI_DOMAIN:-jitsi.example.com}

# Media IP addresses (required for external access)
# IMPORTANT: Change this to your server's public IP
DOCKER_HOST_ADDRESS=192.168.1.100

# Directory for config
CONFIG=~/.jitsi-meet-cfg

#
# Basic configuration options
#

# Image version - recommended to use stable instead of 'unstable'
JITSI_IMAGE_VERSION=stable-9364

# Exposed HTTP port
HTTP_PORT=8000

# Exposed HTTPS port
HTTPS_PORT=8443

# XMPP parameters
XMPP_DOMAIN=meet.jitsi
XMPP_AUTH_DOMAIN=auth.meet.jitsi
XMPP_BOSH_URL_BASE=http://prosody:5280
XMPP_GUEST_DOMAIN=guest.meet.jitsi
XMPP_MUC_DOMAIN=muc.meet.jitsi
XMPP_INTERNAL_MUC_DOMAIN=internal-muc.meet.jitsi
XMPP_RECORDER_DOMAIN=recorder.meet.jitsi
XMPP_SERVER=prosody
XMPP_PORT=5222

# JVB configuration
JVB_BREWERY_MUC=jvbbrewery@internal-muc.meet.jitsi
JVB_PORT=10000
JVB_TCP_HARVESTER_DISABLED=true
JVB_TCP_PORT=4443
JVB_TCP_MAPPED_PORT=4443
JVB_STUN_SERVERS=meet-jit-si-turnrelay.jitsi.net:443

# JICOFO configuration
JICOFO_AUTH_USER=focus

# JVB configuration
JVB_AUTH_USER=jvb

#
# Let's Encrypt configuration (uncomment to enable)
#
# Enable Let's Encrypt certificate generation
# ENABLE_LETSENCRYPT=1

# Domain for which to generate the certificate
# LETSENCRYPT_DOMAIN=meet.example.com

# E-Mail for receiving important account notifications (mandatory)
# LETSENCRYPT_EMAIL=alice@example.com

# Use the staging server (uncomment for testing)
# LETSENCRYPT_USE_STAGING=1

#
# Portainer configuration
#

# Portainer version
PORTAINER_VERSION=2.19-alpine

# Portainer HTTP port
PORTAINER_HTTP_PORT=9000

# Portainer HTTPS port  
PORTAINER_HTTPS_PORT=9443

#
# Advanced configuration options
#

# STUN servers used to discover the server's public IP
STUN_SERVERS=meet-jit-si-turnrelay.jitsi.net:443

# Authentication type (internal, jwt, ldap)
AUTH_TYPE=internal

# Enable authentication
ENABLE_AUTH=0

# Enable guest access
ENABLE_GUESTS=1

# Enable welcome page
ENABLE_WELCOME_PAGE=1

# Enable prejoin page
ENABLE_PREJOIN_PAGE=0

# Restart policy
RESTART_POLICY=unless-stopped

# Enable recording
ENABLE_RECORDING=0

#
# Network configuration
#

# Network name (should match existing external network)
NETWORK_NAME=jitsi-network

# Use external network (set to true to use existing network)
NETWORK_EXTERNAL=true

#
# Traefik configuration
#

# Jitsi domain for Traefik routing
JITSI_DOMAIN=jitsi.example.com